/** A simple class having a {@link RegExp regex} applicable to find something from an url */
export interface ClassWithUrlRegex {

    /** A {@link RegExp regex} to identify the type in an url */
    readonly URL_REGEX: RegExp

}
