import React       from "react";

import AbstractApp from "./AbstractApp";

export default class EveryEntityGameStylesApp
    extends AbstractApp {

    protected _mainContent(): JSX.Element {
        return <>every entity game styles</>;
    }

}