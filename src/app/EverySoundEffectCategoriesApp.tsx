import AbstractApp from './AbstractApp';

/**
 * @reactComponent
 */
export default class EverySoundEffectCategoriesApp
    extends AbstractApp {

    protected _mainContent(): JSX.Element {
        return <>every entity sound effect categories</>;
    }

}